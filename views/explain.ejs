<%- include('layout', { body: `<div class="content">

    <div>
        <h2 style="text-align: center;font-size: 30px; padding-top: 50px;">Giải thích code
        </h2>
        <h1 style="font-size: 25px;">Mẫu câu hỏi</h1>
        <div class="copy-content">
        <p>
            1. Bạn có thể giải thích hàm này làm gì không: [đoạn mã]?
        </p><br>
           <p>2. Tôi gặp khó khăn trong việc hiểu lớp trong code này. Bạn có thể giải thích được không: [đoạn mã]?
        </p><br>
           <p>3. Bạn có thể chia nhỏ vòng lặp này và giải thích chức năng của nó không: [đoạn mã]?
        </p><br>
            <p>4. Bạn có thể chia nhỏ cách hoạt động của chức năng [ngôn ngữ] này không: [đoạn mã]?
            </p><br>
           <p>5. Hàm đệ quy này làm gì: [đoạn mã]?
        </p><br>
           <p>6. Hãy giúp tôi hiểu đoạn code này làm gì: [đoạn mã].
        </p><br>
           <p>7. Bạn có thể giải thích logic đằng sau hàm này không?: [đoạn mã] </p><br>
           <p>8. Bạn có thể giải thích cách triển khai thuật toán này không?: [đoạn mã] 
        </p><br>
            <p>9. Hãy giúp tôi hiểu cách hoạt động triển khai cấu trúc dữ liệu này: [đoạn mã].
            </p><br>
           <p> 10. Tôi đang gặp khó khăn để hiểu khối code sau: [đoạn mã]. Bạn có thể chia nhỏ nó ra cho tôi không, đặc biệt là phần nó triển khai [thuật toán] này không?
        </p><br>
          <p> 11. Bạn có thể giúp tôi hiểu tập lệnh này  không?: [đoạn mã]</p><br>
           <p>12. Giải thích chức năng của đoạn code này để làm gì: [đoạn mã].
        </p><br>
           <p>13. Phần code này có tác dụng gì: [đoạn mã]?
        </p><br>
           <p>14. Bạn có thể hướng dẫn tôi quy trình của tập lệnh code này không?: [đoạn mã] </p><br>
           <p>15. Vui lòng giải thích chức năng của khối code sau đây: [đoạn mã] và cách nó tương tác với các thành phần hệ thống.
        </p><br>
            <p>16. Bạn có thể giải thích chức năng của thuật toán này: [đoạn mã] và kết quả đầu ra dự kiến ​​của nó cho các đầu vào nhất định: [ví dụ đầu vào] không?
            </p><br>
           <p>17. Chia nhỏ lớp code này: [đoạn mã] và giải thích cách các phương thức của nó hoàn thành [nhiệm vụ].
        </p><br>
           <p> 18. Bạn có thể giải thích cách hoạt động của chức năng của đoan code: [đoạn mã] này không? Đặc biệt là nó sử dụng tính năng cụ thể để hoàn thành [nhiệm vụ] như thế nào?
        </p>
        </div>
    </div>

    <script>
        var questions = document.querySelectorAll('.content p');

        questions.forEach(function(question, index) {
            // Đặt id cho mỗi phần tử p là "question" + số thứ tự
            question.id = "question" + (index + 1);
        });

        questions.forEach(function(question) {
            // Lấy id của mỗi phần tử p
            var questionId = question.id;

            // Lấy phần tử p tương ứng với id
            var questionElement = document.getElementById(questionId);

            // Tạo một nút "Copy"
            var copyButton = document.createElement("button");
            copyButton.textContent = "Sao chép";
            copyButton.className = "copy-button";

            // Tạo một hàm onClick để sao chép nội dung khi nút được nhấp vào
            copyButton.onclick = function() {
                copyText(questionId, copyButton);
            };

            // Thêm nút "Copy" vào sau phần tử p
            questionElement.parentNode.insertBefore(copyButton, questionElement.nextSibling);
        });

        // Hàm sao chép nội dung
        function copyText(elementId, button) {
            var text = document.getElementById(elementId).innerText;
            var textarea = document.createElement("textarea");
            textarea.value = text;
            document.body.appendChild(textarea);
            textarea.select();
            document.execCommand("copy");
            document.body.removeChild(textarea);
            button.textContent = "Đã sao chép";

            // Sau 3 giây, chuyển nút đã sao chép về nút sao chép bình thường
            setTimeout(function() {
                button.textContent = "Sao chép";
                button.disabled = false;
            }, 3000);
        }
    </script>

    <script src="script.js"></script>
    ` }) %>