<%- include('layout', { body: `<div class="content">

    <div>
        <h2 style="text-align: center;font-size: 40px;">Tối ưu code</h2>
        <h1>Mẫu câu hỏi</h1>
        <div class="copy-content">
        <p>1. Đề xuất cho tôi cải tiến để tối ưu hóa hàm này: [đoạn mã].
        </p><br>
            <p>2. Bạn có thể cung cấp phiên bản hiệu quả hơn của thuật toán này không: [đoạn mã] 
            </p><br>
            <p>3. Làm cách nào tôi có thể cải thiện hiệu suất của tập lệnh code này: [đoạn mã]?
            </p><br>
           <p> 4. Hàm này: [đoạn mã] đang chạy chậm hơn tôi mong muốn. Cho tôi xin đề xuất tối ưu hóa đi.
        </p><br>
           <p>5. Tôi cần cải thiện tốc độ của thuật toán của đoạn code này: [đoạn mã]. 
        </p><br>
           <p> 6. Làm cách nào tôi có thể làm cho mã xử lý dữ liệu của code này hiệu quả hơn: [đoạn mã]?
        </p><br>
           <p>7. Hàm sau: [đoạn mã] chạy chậm hơn dự kiến ​​khi xử lý [loại đầu vào]. Cho tôi xin đề xuất nào để tối ưu hóa nó đi.
        </p><br>
           <p> 8. Làm cách nào tôi có thể cải thiện hiệu suất của hàm này này: [đoạn mã] khi xử lý tập dữ liệu lớn?
        </p><br>
            <p>9. Cung cấp đề xuất tối ưu hóa cho mã sau: [đoạn mã] được sử dụng để xử lý [loại dữ liệu].
            </p><br>
           <p>10. Làm cách nào tôi có thể tối ưu hóa hàm này: [đoạn mã] để thực hiện [tác vụ] nhanh hơn khi xử lý kích thước dữ liệu lớn và duy trì độ chính xác của [yêu cầu độ chính xác]?
        </p><br>
            <p>11. Tôi có hàm sau: [đoạn mã]. Nó hoạt động như mong đợi nhưng chạy chậm hơn tôi muốn khi xử lý [dữ liệu cụ thể]. Cho tôi xin đề xuất nào để cải thiện hiệu suất đi.
            </p><br>
            <p>12. Code sau: [đoạn mã] thực hiện [nhiệm vụ]. Tuy nhiên, nó có vẻ không hiệu quả với [kiểu dữ liệu] có kích thước [kích thước dữ liệu]. Làm thế nào tôi có thể tối ưu hóa nó đây?
            </p>
        </div>
    </div>

    <script>
        var questions = document.querySelectorAll('.content p');

        questions.forEach(function(question, index) {
            // Đặt id cho mỗi phần tử p là "question" + số thứ tự
            question.id = "question" + (index + 1);
        });

        questions.forEach(function(question) {
            // Lấy id của mỗi phần tử p
            var questionId = question.id;

            // Lấy phần tử p tương ứng với id
            var questionElement = document.getElementById(questionId);

            // Tạo một nút "Copy"
            var copyButton = document.createElement("button");
            copyButton.textContent = "Sao chép";
            copyButton.className = "copy-button";

            // Tạo một hàm onClick để sao chép nội dung khi nút được nhấp vào
            copyButton.onclick = function() {
                copyText(questionId, copyButton);
            };

            // Thêm nút "Copy" vào sau phần tử p
            questionElement.parentNode.insertBefore(copyButton, questionElement.nextSibling);
        });

        // Hàm sao chép nội dung
        function copyText(elementId, button) {
            var text = document.getElementById(elementId).innerText;
            var textarea = document.createElement("textarea");
            textarea.value = text;
            document.body.appendChild(textarea);
            textarea.select();
            document.execCommand("copy");
            document.body.removeChild(textarea);
            button.textContent = "Đã sao chép";

            // Sau 3 giây, chuyển nút đã sao chép về nút sao chép bình thường
            setTimeout(function() {
                button.textContent = "Sao chép";
                button.disabled = false;
            }, 3000);
        }
    </script>

    <script src="script.js"></script>
    ` }) %>